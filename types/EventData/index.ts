// import { StaticImageData } from "next/image";

export type EventDataType = {
  eventName: string;
  eventCompanyName: string;
  // eventCompanyLogo: StaticImageData;
  eventDescription: string;
  eventDate: string;
  eventTime: string;
  eventLocation: string;
  eventLink: string;
};
